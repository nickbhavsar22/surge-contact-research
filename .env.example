# Surge Contact Research — Environment Variables
# Copy this file to .streamlit/secrets.toml (Streamlit format) or export as env vars.

# Required: Password to access the app (checked via SHA256 hash)
app_password = "your_password_here"

# Optional: Hunter.io API key for email enrichment (free tier: 50 searches/month)
# Without this, contact enrichment still works via website scraping only.
hunter_api_key = ""
